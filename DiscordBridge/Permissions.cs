namespace DiscordBridge
{
	internal static class Permissions
	{
		internal static string Use => "discord.use";

		internal static string AcceptInvite => "discord.server.invite";

		internal static string Reconnect => "discord.server.reconnect";

		internal static string Disconnect => "discord.server.disconnect";

		internal static string ReloadConfig => "discord.config.reload";

		internal static string SetBotToken => "discord.config.token";
	}
}
